/target
mutants.out/
mutants.out.old/

# Test-cluster runtime artefacts. SVG plots in docs/cluster/ ARE committed —
# they're the README-facing telemetry. Everything else is regenerated by
# topology.py / run.sh / test_rotation.sh and contains test-only private
# keys and large CSV scrapes.
/tests/cluster/configs/
/tests/cluster/docker-compose.yml
/tests/cluster/docker-compose.rotation.yml
/tests/cluster/out/
/tests/cluster/rotation/
/tests/cluster/pub_keys.json
/tests/cluster/.probe.toml
