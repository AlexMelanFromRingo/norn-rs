# Generated from the project's virtualenv.
# `dependencies` lists what was installed directly; the full resolved
# set, including everything pulled in behind them, is requirements.txt.
[project]
name = "cluster"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "cryptography==48.0.0",
    "matplotlib==3.10.9",
    "networkx==3.6.1",
]
